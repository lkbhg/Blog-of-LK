# 校园网IPv6直连指南.

---

## 版权声明

© 2026 [Kan Liu]。保留所有权利。

本文为原创作品，未经允许不得转载。可以通过以下方式联系作者：[lkbhg@outlook.com]。

## 💡 背景与问题描述

校园网长期以来宣传`IPv6`免流以及国际互联网访问，而对于`IPv4`则存在限速限流的问题。但对于不同的学校以及校园，双栈网络的部署和计费系统千差万别。

而且由于某些情况下，`IPv6`的使用方式被刻意的干扰和隐瞒了，这就导致`IPv6`的使用感受不佳。

为此我通过大量的测试，获得了如下经验。

## IPv6直连

这里以`BIT`的校园网为例，在接入校园网之后，即使不登录深澜系统，也会自动获得一个`2001`开头的`/128`地址。
但获得了这个地址之后，也无法直接访问外网。这里主要有两个原因：

1. 校园网只提供了`IPv4 DNS`,
2. 校园网有`IPv6`入站防火墙

因此，即使自有`IPv6`地址，也无法获得对应的网络服务。必须要先接入系统，获得校园网授权，使用校园网内部的`IPv4 DNS`解析地址之后，才能访问支持`IPv6`的网站。

（而且这个`DNS`服务器经常出问题）

而且，外部也无法直接访问这个`IPv6`地址。除非依赖于一些支持`IPv6`的隧道工具（`Zerotier`）或者代理工具。但从实际的测试来看，在前两个限制下，这些工具也很难使用。

## ipv6 DNS + NAT64
<a name="unique-nat64"></a>
但不必灰心，通过公共`IPv6 DNS`和`NAT64`服务也可以直接使用`IPv6`。这就需要在系统中手动配置`IPv6`的`DNS`，并且这些`IPv6 DNS`的服务商必须要具备`NAT64`的服务。

比如像阿里云这种仅有`IPv6 DNS`的服务商是不可用的。可以直接搜索`Public NAT64`相关信息并配置对应的`IPv6 DNS`

就我目前的测试来看，仅有一些国外`IP`提供这种公共服务。

在配置好之后，即使不登录深澜系统，也可以访问一些`IPv6`网站，实现`IPv6-only`的效果。

但不得不说的是，这种方法除了上一些`bing`和`baidu`这种网站，还是有很多局限性，尤其是对于一些应用软件。

虽然宣传支持`IPv6`，但依然要依赖于`IPv4`的支持，无法实现`IPv6-only`。

## BT IPv6 tracker
由于存在入站防火墙，`IPv6`的直连效果无法发挥真正的价值，DHT这种功能是无论如何都无法打通的。

这里就只剩下使用`IPv6` tracker来实现纯`IPv6 BT`下载的办法了。

但是，`IPv6 tracker`我目前能搜到的很少，而且即使连上了`tracker`， `Peers`可能也没有`IPv6`。

但有时候还是有点（只有一点）用的，尤其是对于`VCB-Studio`这种有`IPv6 SeedBox`的发布组（有机会我一定提供支持）。

## 迅雷

迅雷自己的`tracker`是没有`IPv6-only`支持的，只有迅雷云盘有`IPv6`支持，这一点已经测试过很多次。

只要我使用`IPv6-only`网络连接迅雷`BT`，都会直接断联，但在匹配`Peers`时，有时候又会匹配到`IPv6`用户，这和大多数的`tracker`没什么区别。

但是迅雷云盘的离线后下载是支持`IPv6`的。但它往往需要通过`IPv4/IPv6`双栈同时连接。

虽然在短暂的时间里，`IPv6-only`也可以直接下载，但速度被卡死在100Mbps。并且随着时间的推移，`IPv4`持续断联，必然导致`IPv6`断联。推测是迅雷的某些服务依赖于`IPv4`的间隔时间认证。

(补充：后续经过研究发现，迅雷网盘的限速也是`IPv4+IPv6`双栈进行的，一般会保持`IPv4`和`IPv6`的速度趋于相同）

## 百度云盘

目前能够真正全栈支持`IPv6`的软件，唯一的缺点就是离线下载功能烂完了。连原始的文件目录都保存不了，而且单`BT`文件总数过大就会出错。更不要提各种超时和资源封锁。

除去这些之外，它是真的能通过`IPv6`跑满1000M网络，像是把服务器放在内网一样（比`BIT Mirror`还快），让人非常震惊。

不仅是先离线再取回，还是直接下载到本地，都能跑满`IPv6`网络（就是不知道是因为在打擦边球还是真的技术烂）。

所以对于不需要原始文件目录的下载，百度云盘是真的良心。

## PikPak+NAT4/IPv6回传：比较适合的解决方案

虽然也有很多不足，但也算是在当前情况下，比较适合的终极解决方案。首先先说缺点，尽管在官网上说明了具备`IPv6`功能，但在实际的`DNS`探测中，无法识别任何域名`AAAA记录`，而且和地区无关。

并且，其提供的`Webdav`接口虽然可以跨过防火墙，但估计是由于`Qos`限速以及各种原因，速度实在是太慢了，且非常不稳定，几乎可以直接放弃。

还有一点，由于文件夹的递归下载，其下载过程中往往会由于小文件数目过多而掉速。并不像传统`BT`那样可以整块下载。

不过，由于依托于阿里云国际部署，其`IPv4`地址在全球都具有极快的下载速度。我认为如果不是因为在`IPv6`跨境以及校园网的物理带宽极限的限制下，其速度可以更快。在这一点上，远超迅雷和百度网盘。

并且，不会无缘无故屏蔽正常资源。这一点，也远超全部的网盘。

所以最后只能通过购买`IPv6-only`的代理或者自建`NAT4+IPv6+Hy2`的方式来下载`PikPak`的内容。这样的话费用又会增加一些，但通过捡漏拍卖的服务器可以适当降低成本。

## 总结

这里不得不感叹，国外大城市的网络环境确实更先进，限制更少，更适合搞信息技术，而且服务商也更良心。

这是测试过后的可以快速下载的地区：`（新加坡，香港，澳门，日本，荷兰，美国，法国，德国）`速度几乎相同。

这更是说明了一个事实，校园网确实通过`IPv6`直接接入了国际互联网，但其通过`DNS`污染等方式阻止了一些访问。

这或许也解释了为什么在关闭代理之后，手机有时也能收到APP推送的通知（存疑）。

而且，在不登录校园网的情况下，只要获取到正确的`IPv6`地址，也可以直接通过配置[NAT64](#unique-nat64)或者`IPv6`代理的方式，来浏览网站。

并且由于校园网在分发`IPv6`地址时只依赖于`Mac`地址，所以可以借助`USB网卡`修改`Mac`地址的方式，在网络中更好地隐藏自己的身份。

## 未来

现在网络的问题算是彻底解决了，也不用再去考虑在校园网中使用`Wireshark`抓取那些明文的登录包（这一点是非常离谱的了）。

但由于硬盘涨价太多，储存就成为了一个巨大的问题，不能愉快的玩耍了。

以后在条件运行的情况下，还是可以玩一玩`SeedBox`。到时候再来补充这篇文档。
